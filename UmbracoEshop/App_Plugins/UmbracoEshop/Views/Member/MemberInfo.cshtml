@{
    var myUser = System.Web.Security.Membership.GetUser();
    if (myUser != null)
    {
      <p>
      @myUser.UserName
      </p>
        <p>
        <a title="Odhlásiť" href="@Url.Action("Logout", "Member", new { area = "OsobnaStranka" })" class="btn btn-default"><i class="fa fa-power-off"></i></a>
        </p>
    }
}

